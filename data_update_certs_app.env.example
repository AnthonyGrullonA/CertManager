# Copia este archivo a `data_update_certs_app.env` (gitignored) y complétalo.
# El .sh lo carga automáticamente. NO subas el .env real al repo (es público).

# --- Django ---
# Producción actual = SQLite (perfil standalone). Cambia si usas MySQL en prod.
DJANGO_SETTINGS_MODULE=config.settings.standalone
# PYTHON=python                       # binario de Python (o ruta a tu venv)
# CERTFORGE_DATA_DIR=/ruta/datos      # dónde vive la BD SQLite (standalone)

# --- Owner de la organización ---
CF_OWNER_EMAIL=jairol_grullon@claro.com.do
CF_OWNER_PASSWORD=Ninguna123@

# --- Organización (opcional) ---
# CF_ORG_NAME=Claro

# --- SMTP de producción (déjalo vacío si lo configuras desde el panel) ---
# CF_SMTP_HOST=smtp.claro.com.do
# CF_SMTP_PORT=587
# CF_SMTP_USER=
# CF_SMTP_PASSWORD=
# CF_SMTP_FROM=certificados@claro.com.do
# CF_SMTP_USE_TLS=1

# --- Certificados ---
# CF_CERT_SOURCE=cert.txt             # ruta al archivo de certificados
# CF_RUN_CHECK=1                      # 1 = correr check_certificates al final (lento)
