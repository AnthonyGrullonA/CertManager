{
  "name": "certforge-web",
  "version": "0.1.0",
  "private": true,
  "description": "Capa web (Forge UI) de CertForge — Tailwind + HTMX",
  "scripts": {
    "build:css": "tailwindcss -i ./static/src/forge.css -o ./static/css/forge.css --minify",
    "watch:css": "tailwindcss -i ./static/src/forge.css -o ./static/css/forge.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "postcss-import": "^16.1.0",
    "tailwindcss": "^3.4.0"
  }
}
